type Query {
  hello: String!

  # Get all posts
  posts: [Post]
}

# A post on the feed
#
type Post {
  text: String!
  image: String
  author: Author!
}

# A content creator. For example, someone who posted a Post.
#
type Author {
  name: String!
  avatar: String!
}
